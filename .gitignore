# Результаты прогонов — содержат внешний IP, адреса резолверов,
# внутренние подсети и имена контейнеров. В репозиторий не кладём.
*.json
!package.json

# Выпуски и сводки, даже обезличенные — публиковать осознанно,
# а не автоматически вместе с кодом.
scan-*.json
vpn-scan*.json
digest*.json
novpn.json
vpn.json

# Временные файлы атомарной записи
*.json.tmp

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Редакторы и ОС
.vscode/
.idea/
*.swp
.DS_Store
